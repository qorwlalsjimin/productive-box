name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.https://gist.github.com/qorwlalsjimin/ }}
          TIMEZONE: Asia/Seoul
